@page "/register"
@layout InventoryPlus.Layout.AuthLayout
@attribute [AllowAnonymous]


<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh; padding: 1.5rem;" @onkeydown="HandleKeyDown">
    <div class="card login-card p-4 p-sm-5 w-100" style="max-width: 450px; animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);">
        <div class="auth-brand">
            <i class="fa-solid fa-cube"></i>
        </div>

        @if (isValidating)
        {
            <h2>Verifying Invite</h2>
            <div class="text-center py-4">
                <span class="spinner"></span>
                <p class="text-muted mt-2">Validating your registration link...</p>
            </div>
        }
        else if (!isTokenValid)
        {
            <h2>Invalid Invite Link</h2>
            <div class="alert alert-danger">
                <i class="fa-solid fa-circle-xmark me-1"></i> @ErrorMessage
            </div>
            <p class="text-muted text-center small">Registration requires a valid invite link from an administrator. Please contact your admin to request a new one.</p>
            <div class="text-center mt-3">
                <a href="contact" class="btn btn-outline-secondary"><i class="fa-solid fa-envelope me-1"></i> Contact Us</a>
                <a href="login" class="btn btn-outline-secondary ms-2"><i class="fa-solid fa-right-to-bracket me-1"></i> Sign In</a>
            </div>
        }
        else
        {
            <h2>Create Account</h2>
            <p class="text-muted text-center" style="margin-bottom: 1rem; margin-top: -0.5rem;">You've been invited to join @AppSettings.CompanyName</p>

            @if (tokenTimeRemaining.HasValue)
            {
                <div class="alert @(tokenTimeRemaining.Value.TotalMinutes < 2 ? "alert-warning" : "alert-info") py-2 small text-center">
                    <i class="fa-solid fa-stopwatch me-1"></i> Link expires in <strong>@tokenTimeRemaining.Value.Minutes min @tokenTimeRemaining.Value.Seconds sec</strong>
                </div>
            }

            @if (!string.IsNullOrEmpty(SuccessMessage))
            {
                <div class="alert alert-success">@SuccessMessage</div>
            }
            @if (!string.IsNullOrEmpty(ErrorMessage))
            {
                <div class="alert alert-danger">@ErrorMessage</div>
            }

            <div class="mb-3">
                <label for="email" class="form-label small fw-medium text-muted">Email</label>
                <input id="email" type="email" @bind="Email" class="form-control" placeholder="you@example.com" />
            </div>

            <div class="mb-3">
                <label for="password" class="form-label small fw-medium text-muted">Password</label>
                <input id="password" type="password" @bind="Password" class="form-control" placeholder="Min 6 characters" />
                @if (!string.IsNullOrEmpty(Password) && Password.Length < 6)
                {
                    <small class="text-danger">Password must be at least 6 characters</small>
                }
            </div>

            <div class="mb-3">
                <label for="confirmPassword" class="form-label small fw-medium text-muted">Confirm Password</label>
                <input id="confirmPassword" type="password" @bind="ConfirmPassword" class="form-control" placeholder="Repeat your password" />
                @if (!string.IsNullOrEmpty(ConfirmPassword) && Password != ConfirmPassword)
                {
                    <small class="text-danger">Passwords do not match</small>
                }
            </div>

            <button @onclick="HandleRegister" class="btn btn-primary w-100" disabled="@(IsLoading || !string.IsNullOrEmpty(SuccessMessage))">
                @if (IsLoading)
                {
                    <span class="spinner"></span> <span>Creating account...</span>
                }
                else
                {
                    <i class="fa-solid fa-user-plus me-1"></i> <span>Create Account</span>
                }
            </button>

            <div class="text-center mt-3 small text-muted">
                <p>Already have an account? <a href="login" class="fw-semibold text-decoration-none" style="color: var(--primary);">Sign in</a></p>
            </div>
        }
    </div>
</div>

